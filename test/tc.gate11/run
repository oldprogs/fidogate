#!/bin/sh

../tr.init

T="../../src/gate/ftn2rfc -v"

echo "tc: copying input packets"
cp ../ti.pkt-mail/*.[pP][kK][tT] pin

echo "tc: running"
$T -c../cf/gate.conf

echo "tc: copying input packets"
cp ../ti.pkt-mail/*.[pP][kK][tT] pin

echo "tc: running"
$T -cconfig
