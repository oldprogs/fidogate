-*- Indented-Text -*-

This directory contains configuration files from the actual
configuration of FIDOGATE at orodruin.fido.de (expect the real passwd
file of course ... ;-).

/etc/fidogate:
    common.conf
    ffx.conf
    ftp.conf
    gate.conf
    toss.conf
    
    aliases
    areas
    hosts
    packing
    passwd
    routing

Here is also an example of the new *single* configuration file
    fidogate.conf

/var/lib/fidogate:
    areas.bbs
    fareas.bbs
